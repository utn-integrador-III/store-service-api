version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port${PORT:-8000}